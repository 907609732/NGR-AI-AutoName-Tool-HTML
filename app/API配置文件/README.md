# API 配置文件说明

这个文件夹专门放本机 API 配置，方便打包或单独发给别人。

- `local-config.js`：你自己的真实 API 配置文件，包含视觉 AI、百度翻译、文本翻译模型等密钥。这个文件只建议放在自己电脑上，不要发给不信任的人。
- `local-config.example.js`：配置模板，可以复制一份改名为 `local-config.js` 后填写。

给别人使用时，可以把这个文件夹一起发给对方。对方如果没有你的密钥，也可以复制模板自己填写。
